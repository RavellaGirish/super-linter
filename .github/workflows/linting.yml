name: Linter

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]
    
  lint:
    build:
	   
	   
	   steps:
	   - uses: actions/checkout@v2
	   - name: Super-Linter
             uses: github/super-linter@v4.9.5
             env: 
	       GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
